import React from 'react'
import Image from 'next/image'

const CustomButton = () => {
  return (
    <button
    disabled ={false}
    type={"button"}
    className={`custon-btn`}
    onClick={()=>{} }
    >

    </button>
  )
}

export default CustomButton